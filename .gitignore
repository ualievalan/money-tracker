# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
.env

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Fastlane — Ruby gems (не нужны в git, ставятся через bundle install)
android/vendor/
android/.bundle/

# Fastlane — секретные ключи (никогда не коммитить!)
android/fastlane/firebase-service-account.json
android/fastlane/report.xml

# Keystore и подписи (секреты)
android/app/upload-keystore.jks
android/key.properties

# Firebase / Google — секретные ключи и base64 файлы
*.json
!pubspec.json
firebase-deploy-key-base64*.txt
upload-keystore-base64.txt
google-services-base64.txt

# VS Code — личные настройки запуска не коммитим, но settings.json коммитим
.vscode/launch.json

# Devtools
devtools_options.yaml

# Dockerfile (CI only, опционально)
Dockerfile
Dockerfile.ci
